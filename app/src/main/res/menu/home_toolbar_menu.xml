<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/home_toolbar_search"
        android:title="search"
        android:icon="@drawable/toolbar_menu_search"
        app:showAsAction="always"/>

    <item
        android:id="@+id/home_toolbar_category"
        android:title="category"
        android:icon="@drawable/toolbar_menu_category"
        app:showAsAction="always"/>

    <item
        android:id="@+id/home_toolbar_notify"
        android:title="notify"
        android:icon="@drawable/toolbar_menu_notify"
        app:showAsAction="always"/>
</menu>